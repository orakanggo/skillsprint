import Link from 'next/link'
import { prisma } from '@/lib/prisma'
export default async function Dashboard() { const paths = await prisma.path.findMany({ include: { lessons: true }, orderBy: { title: 'asc' } }); return <main className="dashboard"><nav><Link href="/" className="logo">SkillSprint</Link><Link href="/" className="secondary">Back to home</Link></nav><section className="section"><span className="badge">YOUR LEARNING HUB</span><h1>Pick a path. Build momentum.</h1><p>Three focused sprints are waiting for you.</p><div className="grid">{paths.map(path=><article key={path.id}><span className="badge">{path.category}</span><h2>{path.title}</h2><p>{path.description}</p><p>{path.lessons.length} sprints · 5 minutes each</p><Link className="button" href={`/learn/${path.slug}`}>Start path →</Link></article>)}</div></section></main> }
