import Link from 'next/link'
import { prisma } from '@/lib/prisma'
export default async function PathDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const path = await prisma.path.findUnique({ where: { slug }, include: { lessons: { orderBy: { order: 'asc' } } } }); if (!path) return <main className="section"><h1>Path not found</h1></main>; return <main><nav><Link href="/paths" className="logo">SkillSprint</Link></nav><section className="section"><span className="badge">{path.category}</span><h1>{path.title}</h1><p>{path.description}</p>{path.lessons.map(lesson=><article className="lesson-list" key={lesson.id}><h2>{lesson.title}</h2><p>{lesson.content}</p><Link href={`/learn/sprints/${lesson.id}`} className="button">Start sprint</Link></article>)}</section></main> }
