import Link from 'next/link'
export default function Paths() { return <main><nav><Link href="/" className="logo">SkillSprint</Link></nav><section className="section"><span className="badge">SKILL PATHS</span><h1>Choose your next edge.</h1><p>Practical paths for engineering, product, and AI teams.</p><div className="grid">{['engineering','product','ai'].map(slug=><article key={slug}><h2>{slug[0].toUpperCase()+slug.slice(1)}</h2><p>Build durable skills through focused daily sprints.</p><Link href={`/paths/${slug}`} className="button">View path</Link></article>)}</div></section></main> }
