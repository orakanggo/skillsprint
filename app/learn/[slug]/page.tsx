import Link from 'next/link'
import { prisma } from '@/lib/prisma'
export default async function Learn({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const path = await prisma.path.findUnique({ where: { slug }, include: { lessons: { orderBy: { order: 'asc' } } } }); if (!path) return <main className="section"><h1>Path not found</h1><Link href="/dashboard">Return to dashboard</Link></main>; return <main><nav><Link href="/dashboard" className="logo">SkillSprint</Link><Link href="/dashboard" className="secondary">All paths</Link></nav><section className="section"><span className="badge">{path.category}</span><h1>{path.title}</h1><p>{path.description}</p><div className="lesson-list">{path.lessons.map((lesson, index) => <article key={lesson.id}><div><small>SPRINT {index + 1} · {lesson.duration} MINUTES</small><h2>{lesson.title}</h2><p>{lesson.content}</p></div><Link href={`/learn/${path.slug}/${lesson.id}`} className="button">Start sprint</Link></article>)}</div></section></main> }
