# Agent Instructions

Run `npm install`, `npm run db:migrate`, `npm run db:seed`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` after changes. SQLite lives at `data/skillsprint.db`; do not commit generated secrets, the SQLite database, or its journal.
