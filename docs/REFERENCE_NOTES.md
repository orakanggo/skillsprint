# Reference Notes

- The supplied `index.html` is retained as the visual reference at the repository root and `public/landing.html`.
- SQLite is the default local database.
- Completion requires a user ID and exact answer; answer values are never returned by APIs.
