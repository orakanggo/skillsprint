# Architecture

Next.js App Router serves pages and route handlers. Prisma provides typed SQLite persistence. Zod validates API input. Server components read paths and sprints; completion writes go through the dedicated route. CSS remains the primary visual system, with Tailwind v4 installed for future utility composition.
